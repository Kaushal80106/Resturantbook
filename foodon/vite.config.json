import { defineConfig } from 'vite'

export default defineConfig({
  build: {
    outDir: 'dist', // Output directory for Vercel
    emptyOutDir: true, // Clean the dist folder before building
  },
  server: {
    host: '0.0.0.0', // Allow Vercel to connect properly
    port: 3000, // The default Vercel port
  }
})
